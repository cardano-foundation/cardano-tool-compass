name: "Kupo"
description: "Fast, lightweight, and configurable Cardano chain indexer that syncs blockchain data by filtering transaction outputs based on addresses. It builds a lookup table with output references, values, datums, and scripts, which can be stored in a SQLite database."
url: "https://github.com/CardanoSolutions/kupo"
